#include <iostream>
#include <climits>
#include <vector>
#include <string>
using namespace std;

int main(void)
{
	int L, N;
	cin >> L >> N;
	vector<int, string> move(3);

	for (int i = 0; i < N; ++i) {
	
	}
	return 0;
}